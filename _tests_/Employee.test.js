const { test, expect } = require("@jest/globals")
const jest = require("jest")// to run test in test file
const Employee =require("lib\employee.js")// imports employee class

test("checks to see what kind of employee is being added", ()=>{
    expect(Employee(name,id,email,).toBe(Employee(name,id,email))

})